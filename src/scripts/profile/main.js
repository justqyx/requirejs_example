define([
    'core',
    './settings/index'
], function(core, settingsController) {

    // create a SettingsController here.

    console.info('--- scripts/profile/main ---');

    return 'profile/main';
});
